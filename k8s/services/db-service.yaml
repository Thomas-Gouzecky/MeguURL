apiVersion: v1
kind: Service
metadata:
    name: db-service
    labels:
        app: db-service

spec:
    selector:
        app: postgres

    ports:
        - name: 5432-5432
          port: 5432
          protocol: TCP
          targetPort: 5432
          
    type: ClusterIP
