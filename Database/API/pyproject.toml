[project]
name = "meguURL-database-API"
version = "1.0.0"
requires-python = ">=3.13.1"
dependencies = [
    "fastapi[standard]>=0.140.0",
    "psycopg[binary]>=3.3.4",
    "sqlmodel>=0.0.39",
]

[tool.fastapi]
entrypoint = "app.main:app"

[dependency-groups]
dev = []
