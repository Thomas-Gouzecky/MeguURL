apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
    name: {{ .Values.certificate.backend.name}}
spec:
    secretName: {{ .Values.backend.tlsSecretName }}
    issuerRef:
        name: {{ .Values.ca.clusterIssuer.name }}
        kind: ClusterIssuer
    dnsNames:
        - {{ .Values.backend.name }}
        - "{{ .Values.backend.name }}.{{ .Release.Namespace }}"
        - "{{ .Values.backend.name }}.{{ .Release.Namespace }}.svc"
        - "{{ .Values.backend.name }}.{{ .Release.Namespace }}.svc.cluster.local"