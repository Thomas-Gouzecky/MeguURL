apiVersion: v1
kind: Service
metadata:
    name: {{ .Values.dbAPI.name }}
    labels:
        app: {{ .Values.dbAPI.name }}

spec:
    selector:
        app: {{ .Values.dbAPI.name }}

    ports:
        - name: "{{ .Values.dbAPI.service.port }}-{{ .Values.dbAPI.deployment.port }}"
          port: {{ .Values.dbAPI.service.port }}
          protocol: {{ .Values.service.protocol }}
          targetPort: {{ .Values.dbAPI.deployment.port }}

    type: {{ .Values.service.type }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
    name: {{ .Values.dbAPI.name }}
    labels:
        app: {{ .Values.dbAPI.name }}

spec:
    replicas: {{ .Values.replicaCount }}

    selector:
        matchLabels:
            app: {{ .Values.dbAPI.name }}
    template:
        metadata:
            labels:
                app: {{ .Values.dbAPI.name }}
        spec:
            containers:
                - name: "{{ .Values.dbAPI.name }}-repo"
                  image: "{{ .Values.dbAPI.image.repo }}:{{ .Values.dbAPI.image.tag }}"
                  imagePullPolicy: {{ .Values.imagePullPolicy }}
                  envFrom:
                      - secretRef:
                            name: {{ .Values.dbAPI.secretName }}
                  resources: |
                      {{- toYaml .Values.dbAPI.resources | nindent 20 }}
