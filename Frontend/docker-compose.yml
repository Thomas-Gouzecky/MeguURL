services:
    frontend:
        image: tomcheese/meguurl-frontend-repo:latest
        container_name: meguURL-frontend
        ports:
            - "3000:3000"
        env_file:
            - .env.production
        networks:
            - megu-network

networks:
    megu-network:
